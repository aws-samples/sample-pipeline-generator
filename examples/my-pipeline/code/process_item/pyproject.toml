[tool.poetry]
name = "process-item"
version = "1.0.0"
description = "Lambda step: process a single S3 prefix from the parallel Map fan-out"
authors = ["Amazon Web Services"]
package-mode = false
packages = []

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
boto3 = "1.28.57"
aws-lambda-powertools = "3.31.1"

[tool.poetry.group.test.dependencies]
pytest = "7.4.0"
pytest-cov = "6.3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
